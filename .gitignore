# Go build output
/bin/
*.test
*.out

# VM build artifacts — large, rebuilt locally, never committed
/build/kernel/
/build/rootfs/
/build/guest/eph-agent
*.ext4
*.img
vmlinux*

# runtime / local
*.log
.env
/run/
